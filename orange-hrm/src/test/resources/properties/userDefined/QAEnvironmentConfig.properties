#QA_URL - QA is environment prefix, it can be configures in Environment.properties 
# or can be set to System properties by using System.setProperty("Environment","QA"). This can also be maven parameter
# while using environment specific parameter it can be used as
#String environment = PropertyHolder.generalProperties.get("Environment"); //it will return QA for example.
#String url = PropertyHolder.generalProperties.get(environment+"_URL");  //Life is all about adjustment
QA_URL=https://opensource-demo.orangehrmlive.com/